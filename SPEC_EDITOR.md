# SPEC_EDITOR.md — design-team-hub 에디터(작성폼)

> 개발 시작 전 길잡이. 무엇을 만들고 무엇을 안 만들지를 박아두는 지도.
> **작성 기준**: 2026-05-26 / 초안 / 오늘 오전 팀원(유저) 인터뷰 결과 반영
> **순서 변경 메모**: 원래 뷰어(`SPEC.md`)를 먼저 만들 계획이었으나, 인터뷰 결과 ① 지금 시트가 이미 뷰어 역할을 함 ② 불편함이 대부분 "작성 시"에 발생 → **에디터를 먼저** 만들기로. 뷰어는 뒤로 미룸(폐기 아님).

---

## 목적

팀원이 새 작업을 시트에 직접 안 들어가고 등록할 수 있는 **작성 폼**.
시트로 작성할 때의 고통(행 노가다, 긴 드롭다운, 표기 흔들림)을 없앤다.

핵심 철학: **"틀릴 수 없게 만든 입력 관문."** 시트 직접 입력보다 빠른 걸 노리지 않는다. 빠름이 아니라 **틀림 방지 + 행 노가다 제거**가 가치.

---

## 핵심 전략 — 단계 분리

| 단계 | 내용 | 위험 |
|---|---|---|
| **1단계: 폼만** | 화면(입력 폼)만 만든다. 시트 연결 X. 등록 시 "이런 데이터가 들어갈 것"을 미리보기로 보여줌 | **위험 0** (시트 안 건드림) |
| **2단계: GAS 연결** | 폼이 검증되면 그때 시트 쓰기 연결 | 시트 오염 위험 → 신중히 |

> **왜 이 순서인가**: 제일 위험한 부분(시트 쓰기)을 맨 마지막으로, 그것도 폼을 팀원이 실제로 써보고 "이거다" 한 뒤에 붙인다. 폼만 만들고 갈아엎으면 손해가 가장 싸다. "크게 한 번에 벌이면 작살난다"의 반대.

---

## 입력 항목 (1단계 폼)

**폼 순서는 시트 컬럼 순서를 따르지 않는다.** 시트는 한 행에 입력값·운영값이 섞여 있지만, 폼은 **본인이 메일을 열고 접수하는 사고 흐름**을 따른다. 입력 순서와 저장 위치(시트 컬럼)는 별개 — 입력은 아래 존 순서로 받고, 저장 시에만 각 컬럼에 꽂는다.

### 폼 구조 — 4개 존

```
[메일 존]      메일 제목, 메일 비고
[작성자 존]    광고주(초성 검색), 팀 → AE(담당자)
[마감일 존]    접수일(기본 오늘), 마감일
[작업 존]      온/오프 → 작업유형(종류 → 난이도) → 수량
   └ [+] 소재별 줄 (비고)
```

> 근거(인터뷰): 메일을 열면 **제목과 작성자(AE)**를 먼저 본다. 메일 제목에 "어느 브랜드를 담당하는 어느 팀의 누구"가 담겨 있어, 광고주·팀·AE가 한 덩어리로 파악됨 → 같은 존(작성자 존)에 묶음.
> 앞 세 존은 **기획안 전체 공통**(소재 N개가 다 같음), 작업 존에서 `[+]`로 소재를 쪼갠다.

### 항목 → 시트 매핑

| 존 | 폼 항목 | 시트 | 입력 방식 | 필수 |
|---|---|---|---|---|
| 메일 | 메일 제목 | 비고(J) **셀 메모** | 텍스트 (가장 먼저 입력) | 선택 |
| 메일 | 메일 비고 | (비고 영역) | 텍스트 (동일 제목 메일 구분용) | 선택 |
| 작성자 | 광고주 | 광고주(E) | **초성 검색** (ㅎ → ㅎ 브랜드) | 필수 |
| 작성자 | 팀 | 팀 | 선택 | 필수 |
| 작성자 | 담당자(AE) | 담당자 | **팀 선택 시 그 팀 AE만 연동** | 선택 (예정 건은 비움) |
| 마감일 | 접수일(요청일) | (날짜) | **캘린더, 기본값 오늘** | 필수 |
| 마감일 | 마감일 | 날짜 영역 색칠 | **캘린더** (미래) | 필수 |
| 작업 | 온/오프 | 온오프 | 선택 (online/offline) — **작업유형의 상위 조건** | 필수 |
| 작업 | 작업유형 | 작업유형 | **온오프 → 종류 → 난이도 3단계 연동** | 필수 |
| 작업 | 수량 | 수량(I) | 숫자 | 필수 |
| 작업 | 비고(소재별) | 비고(J) | 텍스트 — `[+]` 복제된 소재마다 개별 | 선택 |
| (별도) | 작업자(디자이너) | 작업자(F) | 선택 — 폼 끝 또는 제외 | 선택 (팀장이 나중 배정) |

**폼에서 제외 (운영값 = 시스템/팀장 몫)**: 상태(기본값 자동) · ID(Scheduler onEdit 자동 발급) · 공유(위젯에서 토글).

---

## 주요 동작

### 1. 광고주 초성 검색 (팀원 1순위 요청)
- "ㅎ" 치면 ㅎ으로 시작하는 브랜드 후보가 좁혀짐 (한글 초성 검색)
- **광고주 마스터 목록** 기반. 목록에서 고르게 해서 표기 통일 (예: "광동침향환"을 "침향환"으로 통일하는 팀 규칙)
- 목록에 없으면 신규 추가 — 확인 한 번 거쳐 무분별한 표기 증식 방지
- **전제: 광고주 마스터 목록이 먼저 있어야 함** (아래 §전제 참고)
- 시트 연결 없이 폼 안에서 동작 → 1단계에서 바로 구현 가능

### 2. 팀 → 담당자(AE) 연동
- 팀(1팀/2팀)을 고르면 그 팀의 AE만 후보로 좁혀짐
- 다른 팀 AE 잘못 고르는 실수 차단
- AE 목록도 고정 → 코드에 하드코딩 (팀별 AE 명단)

### 3. 작업유형 — 온/오프 → 종류 → 난이도 (3단계 연동)
- **온/오프가 상위 조건.** online이냐 offline이냐에 따라 고를 수 있는 작업유형이 **완전히 다름**(겹치는 것 거의 없음) → 온/오프를 먼저 정해야 그에 맞는 종류 목록이 나옴
- 지금 시트 드롭다운이 긴 이유 = **종류와 난이도가 한 줄에 곱해져서** (배너(미니)/배너(심플)/배너(베이직)…)
- 이를 단계로 분리: **온/오프 선택 → 그 세트의 종류 → 그 종류의 난이도**
- 핵심 유형 먼저 보이고, **"전체 보기"로 펼치면** 전사 공용 전체 (팀이 거의 안 쓰는 것도 가끔 필요)
- 체계는 **코드에 하드코딩** (앞으로 바뀔 일 없음). 표 구조: `온라인 → [종류 → 난이도] / 오프라인 → [완전히 다른 종류 → 난이도]` 두 독립 세트
- 시트엔 `종류(난이도)` 형태로 합쳐서 기록 (예: `KV(베이직)`). 난이도 없는 종류는 종류명만.

### 4. 메타 소재 복제 (`[+]` 버튼)
- 기획안 1개 → 소재 N개를 **각각 독립된 행**으로 등록 (소재별 담당 디자이너가 다를 수 있어 쪼개야 함)
- **`[+]` 한 번에 한 줄씩 추가.** 누르면 직전 줄 내용이 복사됨 (광고주·작업유형·마감일 등 공통값 따라옴)
- 복제된 줄에서 **비고는 따로 편집** — 순번(01,02,03)만 붙거나, 그 소재만의 특수 내용이 들어갈 수 있음
- 잘못 추가한 줄은 `[x]`로 삭제
- 마감일: **90%는 소재끼리 같음** → 공통으로 한 번 받고, 차일 넘기는 10%만 해당 줄에서 개별 수정
- 등록 시 각 줄이 독립 행으로 시트에 꽂힘 (각자 UUID 발급)

---

## 안 만들 것 (범위 고정)

- **조회/뷰어 기능** — 팀 전체 타임라인은 별도(`SPEC.md`). 에디터는 쓰기 전용.
- **단가 자동 계산** — 작업유형으로 단가 자동입력 안 함 (현재 그런 연결 없음).
- **상태·ID·공유 입력** — 운영값이라 폼에서 제외.
- **메일 제목 자동 파싱** — 제목에 광고주·팀·AE가 담겨 있지만, **제목 규칙을 안 지키는 경우가 있어** 자동 추출이 어설프게 틀리면 고치는 게 더 일. 각 칸 직접 입력으로 간다. (초성 검색·연동이 있어 손입력도 빠름)

---

## 나중에 (1단계 폼 검증 후 / 2단계 이후)

- **확정 / 예약 구분**: 실제 접수한 일 vs "올 것 같아 예약만 걸어둔 일"(AE 모름)을 구분. 데이터에 상태 속성이 하나 느는 일이라 시트 구조·뷰어와 함께 설계. 1단계 폼엔 미포함.
- **메일 제목 → J열 셀 메모 자동 기입**: `setNote()` 사용. 일반 셀 값 쓰기보다 한 겹 까다로움 → GAS 연결(2단계) 때 구현.
- **마감일 → 날짜 영역 색칠**: 시트의 마감일은 날짜 헤더 열 배경색으로 표현됨. 캘린더로 받은 날짜를 해당 열 색칠로 변환하는 특수 처리 → 2단계.

---

## 기술 구조

- 레포 `design-team-hub` (뷰어와 같은 레포 / 같은 허브)
- **React · Vite · Vercel** (대시보드와 동일 스택)
- 1단계: 시트 연결 없음. 마스터 목록·작업유형 표는 코드/정적 데이터로.
- 2단계: GAS로 `신규·유지보수` 시트에 **쓰기**. UUID는 기존 Scheduler onEdit가 발급.

> ⚠ 에디터가 시트에 **쓰는** 첫 외부 프로그램이 됨. 2단계 진입 시 `DATA_CONTRACT.md`에 "design-team-hub 에디터가 신규·유지보수 시트에 행을 추가(쓰기)한다" 명시하고, 열 위치·UUID 발급 흐름이 기존 자동화(Scheduler)와 충돌하지 않는지 반드시 확인.

---

## 전제 — 광고주 마스터 목록

초성 검색이 작동하려면 "정식 표기 기준 목록"이 먼저 있어야 함. 현재는 팀원 머릿속에만 있음(구전 규칙).

→ **첫 삽**: 업무데이터 시트 광고주 열(F, 8600+행 누적)에서 광고주명을 긁어 중복 제거·빈도순 정렬 → 초안 생성(읽기 전용, 위험 0) → 팀장이 규칙대로 정리(침향환 통일 등). 이게 에디터의 실질적 출발점.

---

## 만드는 순서 (제안)

1. **광고주 마스터 목록 초안 뽑기** (시트 읽기, 위험 0) — 첫 삽
2. 빈 레포 / Vercel / React·Vite 셋업 (껍데기 배포)
3. 폼 골격 — 입력 항목 배치 (시트 연결 X)
4. 광고주 초성 검색 (마스터 목록 기반)
5. 팀→담당자, 작업유형 종류→난이도 연동
6. `[+]` 메타 복제 + 캘린더(요청일·마감일)
7. 등록 시 **미리보기**로 "들어갈 데이터" 보여주기 (시트엔 아직 안 씀)
   → **여기까지 1단계. 팀원이 실제로 써보고 검증.**
8. (검증 후) GAS 연결 — 시트 쓰기, 메일 제목 메모, 마감일 색칠, 확정/예약

→ 한 단계씩 확인하며 진행. 한 번에 다 만들지 않는다.
