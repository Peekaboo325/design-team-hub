# ROADMAP.md — design-team-hub 진행 방향

> 폼 1단계가 끝난 시점에서, 뷰어·편집으로 확장하기 위한 구조 결정을 박아둠.
> **작성 기준**: 2026-05-28 / 대화 정리 (6명 동시 사용 우려에서 출발)
> 관련 문서: [CLAUDE.md](./CLAUDE.md), [DATA_CONTRACT.md](./DATA_CONTRACT.md), [SPEC_EDITOR.md](./SPEC_EDITOR.md), [docs/GAS_REFERENCE.md](./docs/GAS_REFERENCE.md)

---

## 출발한 질문

- 폼 등록을 더 빠릿하게 할 수 없나?
- 6명이 동시에 뷰어를 보고, 편집도 한다면 어떻게 동기화?
- 6명이 공통 데이터를 보려면 항상 구글 시트를 거쳐야 하나?

## 핵심 결정 3가지

1. **시트가 진실(single source of truth)로 남는다.**
   별도 DB·캐시 서버 두지 않음. 위젯·자동화(Scheduler/Sync)·사람이 시트를 동시에 만지는 환경이라, 우리만의 저장소를 끼우면 그 sync가 새로운 약점이 된다. 빚을 늘리는 길.

2. **각 브라우저 안에 "로컬 캐시 + 백그라운드 큐".**
   "가상 서버"는 클라이언트 안에만 존재. 다른 사람과 직접 공유하지 않는다. 모두 시트를 거쳐 만난다.
   - 화면은 로컬 캐시를 본다 → 빠릿함
   - 시트 → 로컬: 폴링(30s + 포커스 시 + 수동 새로고침)
   - 로컬 → 시트: 즉시 큐 적재, 백그라운드 전송, 실패 시 재시도

3. **편집은 행 전체 덮어쓰기 X, 필드 단위 patch.**
   GAS에 `{id, field, value}` 형태 update 엔드포인트. 같은 행 다른 필드 동시 편집 = 자연스럽게 둘 다 반영. 같은 필드 동시 편집 = Last-Write-Wins(나중 사람이 이김). 6명 규모엔 충분.

## 안 가는 길 (그리고 이유)

- **클라이언트끼리 직접 공유 (P2P/WebRTC)** — 6명이 항상 다 켜져 있어야 함. 비현실적.
- **별도 캐시 서버 (Vercel KV/Upstash 등)** — 시트가 살아 있는 한 sync 약점만 추가. 한쪽이 다른 쪽 모르는 순간이 반드시 생김.
- **실시간 DB (Firebase/Supabase) + websocket 푸시** — 6명/일 등록 빈도 낮음 → 30초 폴링으로 충분. 운영 부담만 늘어남.
- **시트 버리고 DB로 갈아타기** — 위젯·자동화 전부 재작성. 우리 권한 밖.
- **rowVersion(updated_at) 컬럼 선제 추가** — DATA_CONTRACT 1번 규칙(앞단 열 이동 = 위젯·자동화 즉사) 위반 비용. 실제 사고 한두 번 터지면 그때 도입.

---

## 단계별 로드맵

### Step 0. 폼 1단계 + GAS 연결 — ✅ 완료
- 4존 폼, 광고주 칩, 팀→AE 연동, 작업유형 3단, `[+]` 메타 복제, 캘린더, 메일 제목 중복 감지
- hub-api.gs v0.9 (create / check-duplicates)
- optimistic UI (즉시 폼 초기화 + 진행 박스 → 성공 박스 → fade)

### Step 1. 폼 미세 다듬기 — (선택, 나중에)
- 진행 박스 → 토스트로 이동 (폼 영역 즉시 다음 입력 가능)
- 메일 제목 중복 체크 결과를 세션 메모리에 캐시
- **체감 답답함이 보고된 후**에 진행. 선제 X.

### Step 2. 뷰어 — ⭐ **진행 중 (2026-05-28 합의)**

**v1 뼈대 (확정)**

- **범위**: 팀 전체 작업 (신규·유지보수 시트 전부). 본인 한정·필터는 나중.
- **모양**: 시트의 가로 날짜 타임라인 유지 + 깔끔한 시각 디자인.
  - 시트와 친숙한 사람이 학습 비용 0으로 적응 가능.
  - "다른 새 뷰"가 아니라 **시트의 친절한 버전**.
- **순서**: 시트 그대로 (선접수 = 위). **광고주·디자이너별 그룹화 X.**
- **시각화 핵심**: 예/대/진/완 4상태가 한눈에 구분되게 (위젯과 동일 체계, 이미 전환 완료).
- **표시 X**: 광고주 그룹 블록, 디자이너별 작업량 요약 — 팀 관리 정보지 운영 정보가 아님.
- **v1은 읽기 전용**. 편집은 Step 3로 분리.

**진입 동선 (확정)**

- **뷰어가 메인 페이지.** 등록은 우측 슬라이드 패널.
- 미적 방향: 페이지 전환이 아니라 **슬라이드가 스르륵 열리고 닫히는** 인터랙션.
- 이 패턴이 Step 3 편집 패널에도 동일하게 재사용됨 (편집 슬라이드 패널).

**기술 결정 (확정 — 진행하면서 보고)**

- **GAS**: `hub-api.gs`에 `?type=list` 액션 신설. 위젯용 `schedule-widget-api.gs`는 "본인 것"만 줘서 재사용 부적합.
- **폴링**: 30초 주기 + 포커스 시 fetch + 수동 새로고침 버튼.
- **로컬 캐시 자료구조**: `Record<id, Row>` 단순형부터. Context로 전역 공유. Zustand 같은 라이브러리는 규모 커진 후 검토.
- **시트 → 캐시 머지 정책**: 시트가 항상 우선. 단 로컬 pending(아직 전송 안 한 편집)은 보존 — Step 3 들어가야 의미 있음, v1은 읽기만이라 단순 덮어쓰기.

**작업 순서**

| # | 작업 | 사용자 컨펌 |
|---|---|---|
| 1 | ROADMAP 갱신 (이 문서) | — |
| 2 | 뷰어 뼈대 + Mock 데이터 (가짜 5~10건으로 행 모양 그리기). 등록 폼은 일단 그대로 메인 | 시안 보고 의견 |
| 3 | 시각 디자인 다듬기 (행 밀도/색/상태 배지/날짜 헤더) | 미세조정 1~2회 |
| 4 | `hub-api.gs`에 `?type=list` 추가 → 새 URL 받아 config 갱신 | 배포 타이밍 |
| 5 | 실 시트 연결 + 폴링 (30s + 포커스 + 수동) | — |
| 6 | 등록 폼 → 슬라이드 패널 이동, 뷰어를 메인으로 | — |

### Step 3. 편집 — Last-Write-Wins
- 흔한 편집부터: 디자이너 재배정, 마감일 변경, 상태 변경
- GAS에 `action=update` 엔드포인트 신설 (`{id, field, value}`)
- 편집 즉시 로컬 캐시 반영 → 화면 빠릿 → 백그라운드 patch
- 같은 행 다른 필드 = 충돌 없음 / 같은 필드 동시 = 나중 사람 이김

### Step 4. 충돌 감지 — (선택, 사고 발생 후)
- 시트에 updated_at 컬럼 추가 (안전한 위치: 완료 시트 Q열 뒤 R열 등)
- 저장 시 버전 비교 → 다르면 "○○님이 먼저 수정함. 새로 불러올까요?" 다이얼로그
- **선제 도입 안 함.** 컬럼 추가가 위젯·자동화에 영향 갈 수 있음 → 사고가 비용을 정당화한 후에.

### Step 5. 실시간 동기 — ❌ 안 함
- websocket / Firebase 등은 우리 규모에 과함. 30초 폴링으로 충분.

## 한 줄 요약

> **폼 다듬기(선택) → 뷰어(+로컬 캐시) → 편집(+필드 patch) → (필요 시) 충돌 감지**

---

## 그림으로

```
┌─ 각 클라이언트 (브라우저, 6명 각자) ──┐
│                                       │
│  로컬 캐시 (가상 서버)                │
│   ├─ 화면은 이걸만 그림 (빠릿)        │
│   ├─ 편집: ID + 바뀐 필드만 큐 적재    │
│   └─ 폴링(30s + 포커스) + 응답 머지   │
└─────────────────┬─────────────────────┘
                  │ 필드 단위 patch + ID 기반
                  ▼
              구글 시트 (진실)
              ↑              ↑
          위젯              자동화 (Scheduler / Sync)
          (공유 토글)        (onEdit, 9시 sync)
```

- 6명이 "공통으로 보는 것"의 정체 = 시트 한 장
- 각자는 그 시트의 옵티미스틱 사본을 자기 브라우저에 들고 있다
- 빠릿함은 "공유 채널이 빠른 것"이 아니라 "각자가 시트를 미리 복사해둔 것"에서 나옴

---

## Step 2 진행 노트

대화로 합의된 원칙·결정은 위 Step 2 섹션에 흡수됨. 이 노트는 진행하며 추가로 박을 결정·관찰 기록용.

- (예정) Mock 시안 보고 행 밀도 결정
- (예정) 시트 데이터 샘플 확인 후 컬럼 폭 정리
- (예정) GAS `?type=list` 응답 구조 확정 (배열? 페이징?)
